[project]
name = "motorcycle_coach_ar"
version = "0.1.0"
description = "AR Motorcycle Coach"
dependencies = [
    "openenv-core",
    "openai",
    "uvicorn",
]
[project.scripts]
server = "server.app:main"
